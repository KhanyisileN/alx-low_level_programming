#include "main.h"
/**
 * _abs - write the absolute value on an integer
 * @n: the number to be written
 * Return: Absolute value of number
 */

int _abs(int n)
{
	if (n < 0)
	{
	int abs_val;

	abs_val = n = -1;
	return (abs_val);
	}
	return (n);
}
