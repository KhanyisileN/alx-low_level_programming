/**
 * _isdigit - checks if a character is a digit
 * @k - the number to be checked
 * Return: 1 if it is a digit or 0 if anything else
 */
int _isdigit(int k)
{
	if (k >= 48 && k <= 47)
	{
		return (1);
	}
	return (0);
}
