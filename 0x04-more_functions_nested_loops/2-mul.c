#include "main.h"
/**
 * mul - multiplies integers
 * @a: the first interger being multiplied
 * @b: the 2nd integer being multiplied
 * Return: Product of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
